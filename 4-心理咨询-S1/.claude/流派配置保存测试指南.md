# 流派配置保存功能测试指南

## 📋 测试步骤

### 1. 准备工作
- [x] 配置服务器已启动（端口8003）
- [x] 修复代码已部署
- [x] 统计修复前字段数：
  - 大观学派等5个标准流派：11个字段
  - 拉康精分（temp_1）：6个字段

### 2. 执行测试

**操作步骤**：
1. 打开配置管理界面
2. 修改任意流派配置（例如修改排序或颜色）
3. 点击"保存所有配置"按钮
4. 观察保存结果

### 3. 验证字段完整性

保存后运行此命令验证：
```bash
cd D:/AI-项目/4-心理咨询-S1
python -c "import json; from pathlib import Path; [(print(f.name, ':', len(json.load(open(f, encoding='utf-8'))), 'fields')) for f in Path('data/config/approaches').glob('*.json')]"
```

### 4. 预期结果

**保存后字段数应该是**：
- daguanpai.json: 12个字段（原11个 + sort_order）
- cbt.json: 12个字段
- psychodynamic.json: 12个字段  
- existential.json: 12个字段
- humanistic.json: 12个字段
- temp_1.json: 6个字段（本来就只有6个）

**保存成功消息**：
- "保存成功并已更新来访者库"
- 详情页自动重新生成
- Tab顺序按新配置显示

---

## ✅ 修复内容

### 核心修改：`src/config_server.py`

**修复逻辑**：
```python
# 保存时合并配置，保留原有字段
if approach_id in existing_configs:
    merged_config = existing_configs[approach_id].copy()
    merged_config.update(approach)  # 只更新界面传来的字段
    final_config = merged_config
```

**保护的字段**：
- description（描述）
- fields（字段定义）
- crisis_levels（危机等级）
- techniques（技术列表）
- metadata（元数据）
- 以及任何其他自定义字段

---

## 🎯 测试场景

### 场景1：修改基本属性
- 修改大观学派的 sort_order: 0 → 1
- 预期：sort_order更新，其他10个字段保留

### 场景2：修改颜色/图标
- 修改拉康精分的颜色为红色
- 预期：color更新，其他5个字段保留

### 场景3：启用/禁用流派
- 启用CBT流派
- 预期：enabled更新为true，其他10个字段保留

---

**配置服务器已启动，现在可以在浏览器中测试保存功能！**
